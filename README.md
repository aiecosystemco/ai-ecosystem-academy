# AI Ecosystem Academy

Private student academy for **Prompt Engineering & AI Content Creation Editing 101** by Daniel Christopher.

Sign in with Google, X, or email. Students pay with Skrill. The author issues a unique code that opens live classes, the full book, studio, and tools — for one person.

Works on phone and computer. One unique code may stay signed in on one phone and one computer at the same time.

## Stack

- TanStack Start (React 19)
- Tailwind CSS v4
- Better Auth (email/password, Google, X)
- Postgres (Neon in production, PGLite locally)

## Run locally

```bash
npm install
npm run dev
```

The app listens on port 8080.

```bash
npm run typecheck
npm run build
```

`DATABASE_URL` is optional in local preview (embedded Postgres). Set it for a real database. Never commit `.env` files.

## Author

The official author Gmail is `aiecosystemco@gmail.com`. Signing in with that address opens the author lock.

## License

Private class material. Not for redistribution.
